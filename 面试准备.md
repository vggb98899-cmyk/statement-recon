# 面试准备：多平台对账引擎

## 项目一句话介绍

> "一个自动核对 Shopify / Amazon / TikTok Shop 订单与 PingPong 支付流水的对账引擎。输入是各平台导出的 CSV，输出是带颜色标记的 Excel 报表 + 自然语言报告 + 飞书告警。"

---

## 面试官可能会问的问题

### Q1：项目背景是什么？为什么要做这个？

**回答方向：** 模拟跨境电商的真实场景——财务每月手工从多个平台导出交易明细，逐一核对 PingPong 到账金额，效率低且容易漏。这个项目就是为了把这个流程自动化。

> **加分点：** 强调你理解业务痛点，不是单纯写代码。

### Q2：多平台数据格式不一样，你怎么处理的？

**回答方向：** 用字段映射字典（field_mapper.py）。每个平台定义一个列名映射表，把 `Total` / `total` / `Gross Amount` 统一成标准字段 `total`。后续所有模块只认标准字段，加新平台只需要在映射字典里加一段。

> **加分点：** 提到"开闭原则"——对扩展开放，对修改封闭。

### Q3：同一订单多个SKU怎么避免重复计算？

**回答方向：** 按订单号分组，用 Pandas 的 `drop_duplicates(subset='order_id')` 把总额去重。明细数据保留用于后续分析，订单级数据用于匹配支付流水。两者分开返回。

> **加分点：** 主动提到"明细和订单级分离"的设计思路。

### Q4：差异怎么归类的？规则是什么？

**回答方向：** 按严重程度自上而下，命中即停：
1. 数据缺失（最严重，优先报）
2. 待查（差异 > 阈值）
3. 手续费差异（差异 ≈ 支付渠道手续费）
4. 匹配

> **加分点：** 提到这个优先级是跟财务主管（业务方）对齐后确定的，说明你有跟业务沟通的能力。

### Q5：为什么不用大模型（DeepSeek）生成报告？

**回答方向：** 财务报告要的是精确和可追溯，不是文采。模板引擎输出稳定，每一句都能追溯到具体数据。大模型适合灵活对话场景，不适合固定格式的业务报告。

> **加分点：** 展示"选型有依据"的能力，不是盲目追新。

### Q6：飞书告警怎么实现的？

**回答方向：** 用 HTTP POST 请求调用飞书群机器人的 Webhook API。对账完成后自动判断：有数据缺失/待查 → 推异常告警；全部正常 → 推"今日对账正常"。未配置 Webhook 时只打印日志，不报错。

> **加分点：** 提到异常处理——网络不通不会导致对账流程崩溃。

### Q7：如果让你加一个新平台（如 Temu），需要改哪些文件？

**回答方向：** 只需要改两个地方：
1. `field_mapper.py` — 加 Temu 的列名映射字典
2. `reconciler.py` — 在平台列表里加一行配置

匹配、归类、报告、告警模块完全不用动。

> **加分点：** 展示模块化设计的优势。

### Q8：Git 提交怎么组织的？

**回答方向：** 按可验收的功能点切分。v1.0 基础对账，v1.1 子标签，v1.2 精细化计算，v3.0 Agent 报告和告警。每次提交都独立可运行。

---

## 面试时的演示流程

建议打开项目，按以下顺序展示：

```
Step 0: 打开项目文件夹          → 展示结构
Step 1: git log --oneline       → 展示迭代历史
Step 2: python src\reconciler.py → 跑一次完整对账
Step 3: 网页查看器              → 展示历史报告
Step 4: 打开一个核心函数          → 展示代码质量
```

每个步骤之间花 10 秒解释"这一步展示什么能力"。

---

## 这个项目展示了你哪些能力

| 能力 | 体现在哪 |
|------|---------|
| **业务理解** | 对账逻辑、净额计算、优先级归类都是跟财务对齐后确定的 |
| **工程习惯** | Git 按功能点切分、模块化设计、异常处理、错误不崩溃 |
| **技术选型** | 模板引擎替代大模型（降本增效）、Pandas 处理 CSV |
| **沟通能力** | 能跟非技术角色（财务主管）对齐需求并迭代落地 |
| **迭代思维** | v1.0 → v3.0，每轮加一个独立可交付的能力 |
